{
  "description": "MemDB memory hooks — only SessionStart (healthcheck) in plugin. Stdin-dependent hooks in ~/.claude/settings.json (plugin stdin bug workaround).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/memdb-healthcheck.mjs",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
